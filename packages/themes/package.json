{
  "name": "mch-themes-test",
  "version": "0.0.3",
  "description": "MCH UI Themes",
  "main": "dist/artbasel/artbase.css",
  "scripts": {
    "build": "npm run css-compile",
    "build-dev": "npm run css-compile && cp -r dist/artbasel ../artbasel-ui-kit/css",
    "css-compile": "node-sass --include-path node_modules --output-style compressed --precision 6 bootstrap -o dist/",
    "watch": "nodemon -e scss -x \"npm run css-compile\"",
    "test": "npm run css-compile"
  },
  "keywords": [
    "sass",
    "css",
    "bootstrap"
  ],
  "author": "",
  "license": "ISC",
  "husky": {
    "hooks": {
      "pre-commit": "npm test"
    }
  },
  "devDependencies": {
    "autoprefixer": "^10.3.7",
    "bootstrap": "^5.1.3",
    "husky": "^7.0.2",
    "node-sass": "^6.0.1",
    "nodemon": "^2.0.13",
    "postcss-cli": "^9.0.1",
    "serve": "^12.0.1"
  }
}
