import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import example from './ArtistCard.example.json';
import ArtistCard from './ArtistCard';

<Meta
  title="Components/Structure/ArtistCard"
  component={ArtistCard}
  decorators={[withDesign]}
/>

export const Template = (args) => <ArtistCard {...args} />;

export const argTypes = {
  cssStyles: {
    table: {
      category: 'Styles',
    },
  },
  cssInternalPrefix: {
    table: {
      category: 'Styles',
    },
  },
  image: {
    table: {
      category: 'ArtistCard',
    },
  },
  artistName: {
    table: {
      category: 'ArtistCard',
    },
  },
  artworks: {
    table: {
      category: 'ArtistCard',
    },
  },
};

export const generalArgs = {
  image: example.image,
  artistName: example.artistName,
  artworks: example.artworks,
};

# ArtistCard

Cards used on a carousel.


<Canvas>
  <Story
    name="ArtistCard"
    args={generalArgs}
    argTypes={{ argTypes }}
    parameters={{
       design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=2794%3A41144',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>


## Properties

<ArgsTable of={ArtistCard} />
