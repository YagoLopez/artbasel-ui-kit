import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import example from './ArtistCard.example.json';
import ArtistCard from './ArtistCard';

<Meta 
  title="Components/Structure/Cards/ArtistCard"
  component={ArtistCard}
  decorators={[withDesign]}
/>

export const Template = (args) => <ArtistCard {...args} />;

export const argTypes = {
  cssStyles: {
    table: {
      category: 'Styles',
    },
  },
  cssInternalPrefix: {
    table: {
      category: 'Styles',
    },
  },
  size: {
    table: {
      category: 'ArtistCard',
    },
  },
  artist: {
    table: {
      category: 'ArtistCard',
    },
  },
  image: {
    table: {
      category: 'ArtistCard',
    },
  },
  description: {
    table: {
      category: 'ArtistCard',
    },
  },
  link: {
    table: {
      category: 'ArtistCard',
    },
  },
  headerLabel: {
    table: {
      category: 'ArtistCard',
    },
  },
  ctaLabel: {
    table: {
      category: 'ArtistCard',
    },
  },
};

export const defaultArgs = {
  artist: example.artist,
  image: example.image,
  link: example.link,
  headerLabel: example.headerLabel,
  ctaLabel: example.ctaLabel,
};

# ArtistCard

These cards make up most of the interface and are key components for logically grouping page content and making it scalable.

## Variants

### Size S

<Canvas>
  <Story
    name="ArtistCard"
    args={{
      description: example.description,
      ...defaultArgs,
    }}
    argTypes={{argTypes}}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=5334%3A48057',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Truncated Text

<Canvas>
  <Story
    name="ArtistCard Truncated Text"
    args={{
      artist: example.artistTruncated,
      description: example.description1050,
      image: example.image,
      link: example.link,
      headerLabel: example.headerLabel,
      ctaLabel: example.ctaLabel,
    }}
    argTypes={{argTypes}}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=5334%3A48057',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable of={ArtistCard} />
