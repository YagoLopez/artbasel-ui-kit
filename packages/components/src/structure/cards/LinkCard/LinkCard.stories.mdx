import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import example from './LinkCard.example.json';
import { Container, Row, Col } from '../../Grid';
import LinkCard from './LinkCard';

<Meta
  title="Components/Structure/Cards/LinkCard"
  component={LinkCard}
  decorators={[withDesign]}
/>

export const Template = (args) => (
  <div style={{ height: '100vh', backgroundColor: '#CCC', padding: '1rem'}}>
    <LinkCard {...args} />
  </div>
);

export const defaultArgs = {
  title: example.title,
  description: example.description,
  showDescription: example.showDescription,
  elasticHeight: example.elasticHeight,
  variant: example.variant,
};

# LinkCard

These type of cards make up most of the interface and are key components for logically grouping page content and making it scalable.

<Canvas>
  <Story
    name="LinkCard"
    args={{ ...defaultArgs }}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url: 'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/branch/1lr8QAkd6z2Tn95cINcb7i/AB---Style-Guide?node-id=11053%3A213644',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Card aligment demostration
These cards would have different height according to their different content. But having the elasticHeight prop set to true, both have the 100% height of its container and get the correct aligment.

<Canvas>
  <Container style={{ backgroundColor: '#ccc', padding: '1rem' }}>
    <Row style={{ height: '200px'}} gutter={'gx-5'}>
      <Col>
        <LinkCard
          {...{
            ...defaultArgs,
            showDescription: true,
            elasticHeight: true,
          }}
        />
      </Col>
      <Col>
        <LinkCard
          {...{
            ...defaultArgs,
            showDescription: true,
            description: 'this is a longer description that could possible end up in more than a single line',
            elasticHeight: true,
          }}
        />
      </Col>
    </Row>
  </Container>
</Canvas>

## Properties

<ArgsTable of={LinkCard} />
