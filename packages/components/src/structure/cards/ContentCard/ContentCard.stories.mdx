import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import example from './ContentCard.example';
import { Container, Row, Col } from '../../Grid';
import ContentCard from './ContentCard';

<Meta
  title="Components/Structure/Cards/ContentCard"
  component={ContentCard}
  decorators={[withDesign]}
/>

export const Template = (args) => <ContentCard {...args} />;

# Content Card

These type of cards make up most of the interface and are key components for logically grouping page content and making it scalable.

## Default

<Canvas className='sb-content-card default'>
  <Story
    name="ContentCard"
    args={{
      ...example,
    }}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=7097%3A128486',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## 1. Variants

### Image
<Canvas>
  <ContentCard {...example} responsive={false} />
</Canvas>

### Video

Content Card with video icon

<Canvas>
  <ContentCard {...example} responsive={false} videoIcon />
</Canvas>

Content Card with video player

<Canvas>
  <ContentCard {...example} responsive={false} videoPlayer />
</Canvas>

## 2. Options

### Subtitle

#### On
<Canvas>
  <ContentCard {...example} responsive contentLink={null}/>
</Canvas>

#### Off
<Canvas>
  <ContentCard {...example} responsive subtitle={null} reverse contentLink={null}/>
</Canvas>

### Text Link

#### On
<Canvas>
  <ContentCard {...example} subtitle={null} responsive />
</Canvas>

#### Off
<Canvas>
  <ContentCard {...example} subtitle={null} responsive contentLink={null} />
</Canvas>

### Reverse Layout

#### Default
<Canvas>
  <ContentCard {...example} responsive />
</Canvas>

#### Reverse
<Canvas>
  <ContentCard {...example} responsive reverse />
</Canvas>

## 3. Behaviors

### Video Poster

When using the video variant, we should place the videos poster image on top of the video container (Youtube, etc.) The poster itself should fade out on click, revealing the video container with controls and begin palying.

<Canvas>
  <ContentCard {...example} responsive videoPlayer />
</Canvas>

<Canvas>
  <Container>
    <Row>
      <Col xs={12} sm={12} md={4} lg={4} className="mb-md-8">
        <ContentCard {...example} responsive={false} videoIcon />
      </Col>
      <Col xs={12} sm={12} md={4} lg={4} className="mb-md-8">
        <ContentCard {...example} responsive={false} videoIcon />
      </Col>
      <Col xs={12} sm={12} md={4} lg={4} className="mb-md-8">
        <ContentCard {...example} responsive={false} videoIcon />
      </Col>
    </Row>
  </Container>
</Canvas>

## Properties

<ArgsTable of={ContentCard} />
