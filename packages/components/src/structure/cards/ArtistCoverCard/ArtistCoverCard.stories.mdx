import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import example from './ArtistCoverCard.example.json';
import ArtistCoverCard from './ArtistCoverCard';

<Meta
  title="Components/Structure/ArtistCoverCard"
  component={ArtistCoverCard}
  decorators={[withDesign]}
/>

export const Template = (args) => <ArtistCoverCard {...args} />;

export const argTypes = {
  cssStyles: {
    table: {
      category: 'Styles',
    },
  },
  cssInternalPrefix: {
    table: {
      category: 'Styles',
    },
  },
  title: {
    table: {
      category: 'ArtistCoverCard',
    },
  },
  description: {
    table: {
      category: 'ArtistCoverCard',
    },
  },
  link: {
    table: {
      category: 'ArtistCoverCard',
    },
  },
};

export const generalArgs = {
  title: example.title,
  description: example.description,
  link: example.link,
};

export const truncatedArgs = {
  title: example.titleLong,
  description: example.descriptionLong,
  link: example.link,
}

# ArtistCoverCard

First Artist card on carousel.

<Canvas>
  <Story
    name="ArtistCoverCard"
    args={generalArgs}
    argTypes={{ argTypes }}
    parameters={{
       design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=2794%3A41144',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Variants

### Truncated Text

<Canvas style={{width: '1500px'}}>
  <Story
    name="ArtistCoverCard Truncated Text"
    args={truncatedArgs}
    argTypes={{ argTypes }}
    parameters={{
       design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=2794%3A41144',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable of={ArtistCoverCard} />
