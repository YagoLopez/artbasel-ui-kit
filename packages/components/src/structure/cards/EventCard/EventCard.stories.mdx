import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import { Container, Row, Col } from '../../Grid';
import example from './EventCard.example';
import EventCard from './EventCard';

<Meta
  title="Components/Structure/Cards/EventCard"
  component={EventCard}
  decorators={[withDesign]}
/>

export const Template = (args) => <EventCard {...args} />;

# Event Card

These type of cards make up most of the interface and are key components for logically grouping page content and making it scalable.

## Layout A

<Canvas>
  <EventCard {...example} responsive={true} tags={null}>
    {Template.bind({})}
  </EventCard>
</Canvas>

## Layout B

<Canvas>
  <EventCard {...example} responsive={false} tags={null}>
    {Template.bind({})}
  </EventCard>
</Canvas>

## Tags

<Canvas>
  <EventCard {...example} responsive={true}>
    {Template.bind({})}
  </EventCard>
</Canvas>

<Canvas>
  <EventCard {...example} responsive={false}>
    {Template.bind({})}
  </EventCard>
</Canvas>

## List Page

<Canvas>
  <Container>
    <Row>
      {Array(6)
        .fill(example)
        .map((card, i) => {
          return (
            <Col key={`col-${i}`} xs={12} sm={12} md={6} lg={4}>
              <EventCard {...example} responsive={false} tags={null} />
            </Col>
          );
        })}
    </Row>
  </Container>
</Canvas>

## Default

<Canvas>
  <Story
    name="EventCard"
    args={{
      ...example,
    }}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/?node-id=5908%3A95033',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable of={EventCard} />
