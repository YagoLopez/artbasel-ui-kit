import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import example from './OrderCard.example.json';
import { Container, Row, Col } from '../../Grid';
import OrderCard from './OrderCard';

<Meta
  title="Components/Structure/Cards/OrderCard"
  component={OrderCard}
  decorators={[withDesign]}
/>

export const Template = (args) => (
  <div>
    <OrderCard {...args} />
  </div>
);

# OrderCard

These cards are used for My Orders List overview.

<Canvas>
  <Story
    name="OrderCard"
    args={{...example}}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url: 'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/branch/1lr8QAkd6z2Tn95cINcb7i/AB---Style-Guide?node-id=11053%3A213644',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable of={OrderCard} />
