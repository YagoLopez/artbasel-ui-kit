import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import { Container, Row, Col } from '../../Grid';
import RoomCard from './RoomCard';
import { example, imagesArray } from './RoomCard.example';

<Meta
  title="Components/Structure/cards/OVR Room Card"
  component={RoomCard}
  decorators={[withDesign]}
/>

export const Template = (args) => <RoomCard {...args} />;

export const argTypes = {
  openingSoon: {
    table: {
      category: 'Tags',
    },
  },
  visited: {
    table: {
      category: 'Tags',
    },
  },
  curator: {
    table: {
      category: 'Tags',
    },
  },
  collection: {
    table: {
      category: 'Collections',
    },
  },
  show: {
    table: {
      category: 'Collections',
    },
  },
  unavailableToView: {
    table: {
      category: 'Collections',
    },
  },
  selectMode: {
    table: {
      category: 'Collections',
    },
  },
};

# OVR Room Card

These type of cards make up most of the interface and are key components for logically grouping page content and making it scalable.

## Types

### Freestanding

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type="freestanding"
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={example.curator}
    show={example.show}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

### Hybrid

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type="hybrid"
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={example.curator}
    show={example.show}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

## Live Chat

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={true}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={example.curator}
    show={example.show}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

## Opening Soon

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={{
      active: true,
      label: example.openingSoon.label,
    }}
    visited={example.visited}
    curator={example.curator}
    show={example.show}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

## Visited

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={{
      active: true,
      label: example.visited.label,
    }}
    curator={example.curator}
    show={example.show}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

## Curator's Pick

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={{
      active: true,
      label: example.curator.label,
    }}
    show={example.show}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

### Displaying more than one tag

If using "Opening Soon" and "Visited" together or more than one except the "Curator's Pick," show them side by side as long as space permits. There should always be 8px of space between these tags, and it's okay for them to render past one line when faced with width constraints.

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={{
      active: true,
      label: example.openingSoon.label,
    }}
    visited={{
      active: true,
      label: example.visited.label,
    }}
    curator={example.curator}
    show={example.show}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

## Dark Theme

### Hybrid

<Canvas style={{ width: '400px', background: '#1D2327' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme="dark"
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={example.curator}
    show={example.show}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

### Freestanding

<Canvas style={{ width: '400px', background: '#1D2327' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme="dark"
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type="freestanding"
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={example.curator}
    show={example.show}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

## Collections

### Default

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type="hybrid"
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={example.curator}
    show={{
      active: true,
      label: example.show.label,
    }}
    unavailableToView={example.unavailableToView}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

## Unavailable to view

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={example.curator}
    show={{
      active: true,
      label: example.show.label,
    }}
    unavailableToView={{
      active: true,
      label: example.unavailableToView.label,
    }}
    selectMode={example.selectMode}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

## Multi-Select

### Default

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={example.curator}
    show={{
      active: true,
      label: example.show.label,
    }}
    unavailableToView={example.unavailableToView}
    selectMode={{
      active: true,
      checked: false,
      disabled: false,
    }}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

### Opening Soon

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={{
      active: true,
      label: example.openingSoon.label,
    }}
    visited={example.visited}
    curator={example.curator}
    show={{
      active: true,
      label: example.show.label,
    }}
    unavailableToView={example.unavailableToView}
    selectMode={{
      active: true,
    }}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

### Unavailable to view

<Canvas style={{ width: '400px' }}>
  <RoomCard
    title={example.title}
    collaboratedAccounts={example.collaboratedAccounts}
    ownerAccount={example.ownerAccount}
    theme={example.theme}
    sectorsData={example.sectorsData}
    linkRenderer={example.linkRenderer}
    roomLink={example.roomLink}
    ownerAccountLink={example.ownerAccountLink}
    type={example.type}
    imageUrl={example.imageUrl}
    liveChat={example.liveChat}
    openingSoon={example.openingSoon}
    visited={example.visited}
    curator={example.curator}
    show={{
      active: true,
      label: example.show.label,
    }}
    unavailableToView={{
      active: true,
      label: example.unavailableToView.label,
    }}
    selectMode={{
      active: true,
      checked: false,
      disabled: false,
    }}
    collection={example.collection}
  >
    {Template.bind({})}
  </RoomCard>
</Canvas>

## List Page

<Preview>
  <div>
    <Container>
      <Row>
        {Array(6)
          .fill(example)
          .map((card, i) => {
            return (
              <Col
                key={`col-${i}`}
                xs={12}
                sm={12}
                md={6}
                lg={4}
                xl={4}
                xxl={4}
                className="mb-lg-8 mb-md-8"
              >
                <RoomCard {...{ ...example, imageUrl: imagesArray[i] }} />
              </Col>
            );
          })}
      </Row>
    </Container>
  </div>
</Preview>

## Default

<Canvas style={{ width: '400px' }}>
  <Story
    name="OVR\ Room\ Card"
    args={example}
    argTypes={argTypes}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=6522%3A115174',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable of={RoomCard} />
