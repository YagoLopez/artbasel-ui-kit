<!--- Megamenu.stories.mdx --->

import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';

import Megamenu from './Megamenu';
import menuData from './Menudata.example.json';

import MegamenuDocumentation from './MegamenuDocumentation.mdx';

<Meta
  title="Navigation/Mega Menu"
  component={Megamenu}
  decorators={[withDesign]}
/>

export const Template = (args) => <Megamenu {...args} />;

export const handleOnSearch = (searchTerm) =>
  console.log('Search term: ', searchTerm);

export const handleOnLogout = () => console.log('Logged Out');

export const languageData = {
  languageHeader: 'Site Language',
  languageSelected: 'en',
  languageEntries: [
    {
      type: 'en',
      label: 'English',
    },
    {
      type: 'fr',
      label: 'Français',
    },
    {
      type: 'cn',
      label: '简体中文',
    },
  ],
  onLanguageClick: (type) => console.log(`You selected ${type}`),
};

export const profileData = {
  entries: [
    {
      type: 'link',
      label: 'My Account',
      link: '/dashboard/my-account',
    },
    {
      type: 'link',
      label: 'My Fairs & Events',
      link: '#',
    },
    {
      type: 'link',
      label: 'My Gallery Dashboard',
      link: '/dashboard/my-gallery-dashboard',
    },
    { type: 'action', label: 'Sign out' },
  ],
  onProfileClick: () => console.log('clicked profile'),
  onProfileOpen: () => console.log('opened profile'),
  onProfileClose: () => console.log('closed profile'),
  onProfileLogout: () => console.log('Logged Out'),
};

export const userData = {
  name: 'Jose Gumpert Ros',
  vipStatus: true,
  isUserLoggedIn: true,
};

export const linkRenderer = (link, label) => <a href={link}>{label}</a>;

<Story
  name="Mega Menu"
  args={{
    menuData: menuData,
    linkRenderer: linkRenderer,
    onSearch: handleOnSearch,
    languageData: languageData,
    userData: userData,
    profileWelcomeHeader: 'Welcome, ',
    profileData: profileData,
    loggedCollectionUrl: 'https://artbasel.com/mycollections',
    unloggedCollectionUrl: 'https://artbasel.com/access',
    collectionButtonTitle: 'My Collections',
    logoVariant: 'basel',
  }}
  parameters={{
    layout: 'fullscreen',
    docs: {
      page: MegamenuDocumentation,
    },
    design: {
      name: 'Figma Design',
      type: 'figma',
      url: 'https://www.figma.com/file/3Aa83xoxTTGnObo9tURiCe/Navigation?node-id=120%3A2165',
    },
  }}
>
  {Template.bind({})}
</Story>
