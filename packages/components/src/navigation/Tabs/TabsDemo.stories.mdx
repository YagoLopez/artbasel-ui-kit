<!--- ModalDemo.stories.mdx --->

import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';

import Tabs from './Tabs';
import Tab from './Tab';

<Meta
  title="Components/Navigation/Tabs"
  component={Tabs}
  decorators={[withDesign]}
/>

export const Template = (args) => (
  <Tabs
    defaultActiveKey="profile"
    id="uncontrolled-tab-example"
    className="mb-3"
    {...args}
  >
    <Tab eventKey="home" title="Home">
      <p className="px-8 py-5 text-small">Home</p>
    </Tab>
    <Tab eventKey="profile" title="Profile">
      <p className="px-8 py-5 text-small">Profile</p>
    </Tab>
    <Tab eventKey="disabled" title="Disabled" disabled>
      <p className="px-8 py-5 text-small">Disabled</p>
    </Tab>
  </Tabs>
);

# Tabs

A set of layered sections of content that are displayed one at a time.

## Default

<Canvas>
  <Story
    name="Default"
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url: 'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=5063%3A49582',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<br />

### Tabs

<ArgsTable of={Tabs} />

### Tab

<ArgsTable of={Tab} />
