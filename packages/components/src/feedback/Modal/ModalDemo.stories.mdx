<!--- ModalDemo.stories.mdx --->

import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';

import ModalDemo from './Modal.demo';
import Modal from './Modal';
import Header from './Header';
import Footer from './Footer';
import Body from './Body';

<Meta
  title="Components/Feedback/Modal"
  component={ModalDemo}
  decorators={[withDesign]}
/>

export const Template = (args) => <ModalDemo {...args} />;

export const argTypes = {
  size: {
    table: {
      category: 'ModalDemo',
    },
  },
  filter: {
    table: {
      category: 'ModalDemo',
    },
  },
};

# Modal

Modals inform users about a task and can contain information, require decisions, or involve multiple tasks.

## Default

<Canvas>
  <Story
    name="Modal Demo"
    args={{
      size: 'sm',
      filter: false,
      title: 'Modal',
      subline: 'Subline (optional)',
      primaryActionText: 'OK',
      secondaryActionText: 'Cancel',
    }}
    argTypes={argTypes}
    parameters={{
      docs: {
        source: {
          code: `const YourComponent = ({ size, filter, title, subline }) => {
  const [show, setShow] = useState(false);
  const handleClose = () => setShow(false);
  const handleShow = () => setShow(true);
  return (
    <>
      <Button variant="primary" onClick={handleShow}>
        Launch demo modal
      </Button>
      <Modal
        show={show}
        size={size}
        onHide={handleClose}
      >
      <Modal.Header title={title} subline={subline} onHide={handleClose} />
        <Modal.Body>
          <p>lorem ipsum</p>
        </Modal.Body>
        <Modal.Footer>
          <Button variant='secondary'>Secondary Button</Button>
          <Button>Primary Button</Button>
        </Modal.Footer>
      </Modal>
    </>
  );`,
        },
      },
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=584%3A27003',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Options

<br />

#### Size

<br />

##### Small (sm)

<Canvas>
  <Story
    name="Modal Small"
    args={{
      title: 'Modal',
      subline: 'Subline (optional)',
      size: 'sm',
      filter: false,
      primaryActionText: 'OK',
      secondaryActionText: 'Cancel',
    }}
    argTypes={argTypes}
    parameters={{
      docs: {
        source: {
          code: `const YourComponent = () => {
  const [show, setShow] = useState(false);
  const handleClose = () => setShow(false);
  const handleShow = () => setShow(true);
  return (
    <>
      <Button variant="primary" onClick={handleShow}>
        Launch demo modal
      </Button>
      <Modal
        show={show}
        size="sm"
        onHide={handleClose}
      >
      <Modal.Header title="Modal" subline="Subline (optional)" onHide={handleClose} />
        <Modal.Body>
          <p>lorem ipsum</p>
        </Modal.Body>
        <Modal.Footer>
          <Button variant='secondary'>Secondary Button</Button>
          <Button>Primary Button</Button>
        </Modal.Footer>
      </Modal>
    </>
  );`,
        },
      },
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=584%3A27003',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

##### Medium (md)

<Canvas>
  <Story
    name="Modal Medium"
    args={{
      title: 'Modal',
      subline: 'Subline (optional)',
      size: 'md',
      filter: false,
      primaryActionText: 'OK',
      secondaryActionText: 'Cancel',
    }}
    argTypes={argTypes}
    parameters={{
      docs: {
        source: {
          code: `const YourComponent = () => {
  const [show, setShow] = useState(false);
  const handleClose = () => setShow(false);
  const handleShow = () => setShow(true);
  return (
    <>
      <Button variant="primary" onClick={handleShow}>
        Launch demo modal
      </Button>
      <Modal
        show={show}
        size="md"
        onHide={handleClose}
      >
      <Modal.Header title="Modal" subline="Subline (optional)" onHide={handleClose} />
        <Modal.Body>
          <p>lorem ipsum</p>
        </Modal.Body>
        <Modal.Footer>
          <Button variant='secondary'>Secondary Button</Button>
          <Button>Primary Button</Button>
        </Modal.Footer>
      </Modal>
    </>
  );`,
        },
      },
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=584%3A27003',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

##### Large (lg)

<Canvas>
  <Story
    name="Modal Large"
    args={{
      title: 'Modal',
      subline: 'Subline (optional)',
      size: 'lg',
      filter: false,
      primaryActionText: 'OK',
      secondaryActionText: 'Cancel',
    }}
    argTypes={argTypes}
    parameters={{
      docs: {
        source: {
          code: `const YourComponent = () => {
  const [show, setShow] = useState(false);
  const handleClose = () => setShow(false);
  const handleShow = () => setShow(true);
  return (
    <>
      <Button variant="primary" onClick={handleShow}>
        Launch demo modal
      </Button>
      <Modal
        show={show}
        size="lg"
        onHide={handleClose}
      >
      <Modal.Header title="Modal" subline="Subline (optional)" onHide={handleClose} />
        <Modal.Body>
          <p>lorem ipsum</p>
        </Modal.Body>
        <Modal.Footer>
          <Button variant='secondary'>Secondary Button</Button>
          <Button>Primary Button</Button>
        </Modal.Footer>
      </Modal>
    </>
  );`,
        },
      },
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=584%3A27003',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

#### Filter Modal

<Canvas>
  <Story
    name="Modal Filter"
    args={{
      title: 'Filter & Sort',
      filter: true,
      subline: 'Subline (optional)',
      primaryActionText: 'Primary Action',
      secondaryActionText: 'Clear all',
    }}
    argTypes={argTypes}
    parameters={{
      docs: {
        source: {
          code: `const YourComponent = () => {
  const [show, setShow] = useState(false);
  const handleClose = () => setShow(false);
  const handleShow = () => setShow(true);
  return (
    <>
      <Button variant="primary" onClick={handleShow}>
        Launch demo modal
      </Button>
      <Modal
        show={show}
        filter
        onHide={handleClose}
      >
      <Modal.Header title="Modal" subline="Subline (optional)" onHide={handleClose} />
        <Modal.Body>
          <p>lorem ipsum</p>
        </Modal.Body>
        <Modal.Footer>
          <TextLink variant='secondary'>Secondary Button</TextLink>
          <Button>Primary Button</Button>
        </Modal.Footer>
      </Modal>
    </>
  );`,
        },
      },
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=584%3A27003',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

##### Vertical Footer

<Canvas>
  <Story
    name="Modal Vertical Footer"
    args={{
      title: 'Modal',
      subline: 'Subline (optional)',
      size: 'sm',
      filter: false,
      primaryActionText: 'OK',
      secondaryActionText: 'Cancel',
      footerOrientation: 'vertical',
    }}
    argTypes={argTypes}
    parameters={{
      docs: {
        source: {
          code: `const YourComponent = () => {
  const [show, setShow] = useState(false);
  const handleClose = () => setShow(false);
  const handleShow = () => setShow(true);
  return (
    <>
      <Button variant="primary" onClick={handleShow}>
        Launch demo modal
      </Button>
      <Modal
        show={show}
        size="sm"
        onHide={handleClose}
      >
      <Modal.Header title="Modal" subline="Subline (optional)" onHide={handleClose} />
        <Modal.Body>
          <p>lorem ipsum</p>
        </Modal.Body>
        <Modal.Footer orientation="vertical">
          <Button variant='secondary'>Secondary Button</Button>
          <Button>Primary Button</Button>
        </Modal.Footer>
      </Modal>
    </>
  );`,
        },
      },
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=584%3A27003',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<br />

### Modal

<ArgsTable of={Modal} />

### Header

<ArgsTable of={Header} />

### Footer

<ArgsTable of={Footer} />

### Body

<ArgsTable of={Body} />
