<!--- ProgressBar.stories.mdx --->

import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';

import ProgressBar from './ProgressBar';

<Meta
  title="Components/Feedback/ProgressBar"
  component={ProgressBar}
  decorators={[withDesign]}
/>

# ProgressBar Field

ProgressBar can take all arguments that you pass in

export const Template = (args) => <ProgressBar {...args} />;

<Canvas>
  <Story
    name="ProgressBar"
    args={{
      percentage: 60
    }}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/WjzXuLNnALX1Qp5XFkbMbo/PLP-Design?node-id=900%3A51155',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Options

### Progress bar at 25%

<Preview>
  <div>
    <ProgressBar percentage={25} width="210px" />
  </div>
</Preview>

<ArgsTable of={ProgressBar} />
