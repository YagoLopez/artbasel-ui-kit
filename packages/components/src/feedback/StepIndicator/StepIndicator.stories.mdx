import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import StepIndicator from './StepIndicator';
import StepIndicatorDemo from './StepIndicator.demo';

<Meta
  title="Components/Feedback/Step Indicator"
  component={StepIndicatorDemo}
  decorators={[withDesign]}
/>

export const Template = (args) => <StepIndicatorDemo {...args}/>;

export const generalArgs = {
  stepActive: 1,
  steps: ['Start', 'Step 1', 'Step 2', 'End'],
}

export const stepIndicatorArgs = {
  activeStep: 1,
  stepLabels: ['Start', 'Step 1', 'Step 2', 'End'],
  onClickStep: () => {}
}

# Step Indicator

A step indicator updates users on their progress through a multi-step process. It’s Interactive and users they can visit steps already completed by clicking on the label or circular device above it.

<Canvas>
  <Story
    name="Step\ Indicator"
    args={generalArgs}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=5340%3A45928',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Default

<Canvas>
  <StepIndicator {...stepIndicatorArgs} />
</Canvas>

## Properties

<ArgsTable of={StepIndicator} />
