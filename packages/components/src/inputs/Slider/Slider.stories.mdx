<!--- Accordion.stories.mdx --->

import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';

import SliderDemo from './Slider.demo';
import Slider from './Slider';

<Meta
  title="Components/Inputs/Slider"
  component={Slider}
  decorators={[withDesign]}
/>

# Slider

export const Template = (args) => <SliderDemo {...args} />;

export const argTypes = {
  onChange: { action: 'onChange' },
  onMouseUp: { action: 'onMouseUp' },
};

<Canvas style={{ padding: '30px' }}>
  <Story
    name="Slider"
    argTypes={argTypes}
    args={{
      min: 0,
      max: 100,
      step: 1,
      isRange: true,
      disabled: false,
      value: [20, 80],
    }}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url: 'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=3469%3A46678',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Variants

### Range

<Canvas style={{ padding: '30px' }}>
  <Slider
    min={0}
    max={100}
    isRange
    step={1}
    value={[25, 75]}
    onChange={([min, max]) => {}}
  />
</Canvas>

### Single

<Canvas style={{ padding: '30px' }}>
  <Slider min={0} max={100} step={1} value={65} onChange={(val) => {}} />
</Canvas>

## States

### Range disabled

<Canvas style={{ padding: '30px' }}>
  <Slider
    min={0}
    max={100}
    isRange
    step={1}
    value={[25, 75]}
    onChange={([min, max]) => {}}
    disabled
  />
</Canvas>

### Single disabled

<Canvas style={{ padding: '30px' }}>
  <Slider
    min={0}
    max={100}
    step={1}
    value={65}
    onChange={(val) => {}}
    disabled
  />
</Canvas>

## Properties

<ArgsTable of={Slider} />
