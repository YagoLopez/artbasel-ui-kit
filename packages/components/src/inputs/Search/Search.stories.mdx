<!--- Search.stories.mdx --->

import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';

import { Search } from '.';

<Meta
  title="Components/Inputs/Search"
  component={Search}
  decorators={[withDesign]}
/>

# Search Field

Search field is a field input used to find something.

export const Template = (args) => <Search {...args} />;

export const argTypes = {
  className: {
    table: {
      category: 'Styles',
    },
  },
  onChange: {
    table: {
      category: 'Events',
    },
  },
};

<Canvas>
  <Story
    name="Search"
    args={{}}
    argTypes={argTypes}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=486%3A18914',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## States

<Canvas>
  <div>
    <b>Default</b>
    <Search placeholder="Search for artworks, events, galleries..." />
  </div>
</Canvas>

<Canvas>
  <div>
    <b>Hover</b>
    <Search
      placeholder="Search for artworks, events, galleries..."
      className="hover"
    />
  </div>
</Canvas>

<Canvas>
  <div>
    <b>Active</b>
    <Search
      placeholder="Search for artworks, events, galleries..."
      className="active"
    />
  </div>
</Canvas>

<Canvas>
  <div>
    <b>Active (typing)</b>
    <Search
      placeholder="Search for artworks, events, galleries..."
      value="Search Text"
      className="active"
    />
  </div>
</Canvas>

<Canvas>
  <div>
    <b>Focus</b>
    <Search
      placeholder="Search for artworks, events, galleries..."
      className="focus"
    />
  </div>
</Canvas>

## Properties

<ArgsTable of={Search} />
