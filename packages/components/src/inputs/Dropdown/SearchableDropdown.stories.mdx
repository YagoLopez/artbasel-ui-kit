import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import { Container } from 'react-bootstrap';

import dropdownOptions from './CountryOptions.example.json';
import { Dropdown } from '.';

<Meta
  title="Components/Inputs/Dropdown/Searchable"
  component={Dropdown}
  decorators={[
    withDesign,
    (storyFn) => <div style={{ height: '250px' }}>{storyFn()}</div>,
  ]}
/>

# Searchable Dropdown field

Searchable Dropdown field is a field used to search and select a value from diferent options

export const Template = (args) => <Dropdown {...args} />;

export const argTypes = {
  className: {
    table: {
      category: 'Styles',
    },
  },
  onChange: {
    table: {
      category: 'Events',
    },
  },
};

<Canvas>
  <Story
    name="Searchable"
    args={{
      label: 'Label',
      options: dropdownOptions,
      isMultiselect: false,
      isSearchable: true,
      placeholder: 'Type a Term',
    }}
    argTypes={argTypes}
    parameters={{
      design: {
        name: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide?node-id=486%3A22447',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## States

<Canvas>
  <div>
    <b>Searchable text selection</b>
    <Dropdown
      label="Label"
      isSearchable
      style={{ height: '250px', width: '240px' }}
      options={dropdownOptions}
      placeholder="Type a Term"
    />
  </div>
</Canvas>

<Canvas>
  <div>
    <b>Helper text</b>
    <Dropdown
      label="Label"
      isSearchable
      helpText="Helper Text"
      options={dropdownOptions}
      style={{ height: '250px', width: '240px' }}
      placeholder="Type a Term"
    />
  </div>
</Canvas>

<Canvas>
  <div>
    <b>Error</b>
    <Dropdown
      label="Label"
      isSearchable
      helpText="Helper Text"
      helpTextType="danger"
      style={{ height: '250px', width: '240px' }}
      options={dropdownOptions}
      placeholder="Type a Term"
    />
  </div>
</Canvas>

## Properties

<ArgsTable of={Dropdown} />
