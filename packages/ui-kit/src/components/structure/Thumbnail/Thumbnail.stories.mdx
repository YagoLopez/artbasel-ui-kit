import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import example from './Thumbnail.example.json';
import Thumbnail from './Thumbnail';
import { Image } from 'react-bootstrap';

<Meta 
  title="Components/Structure/Thumbnail"
  component={Thumbnail}
  decorators={[withDesign]}
/>

export const Template = (args) => <Thumbnail {...args} />;

export const argTypes = {
  src: {
    table: {
      category: 'Thumbnail',
    },
  },
  width: {
    table: {
      category: 'Thumbnail',
    },
  },
  height: {
    table: {
      category: 'Thumbnail',
    },
  },
};

# Thumbnail

The thumbnail is applied to display a small imagen or to use inside a card in order to display images correctly.
It adapts the image size to its container without distortion and centers the image by matching its smallest side
to the container. It zooms in when the image is smaller to avoid having margins.

<Canvas>
  <Story
    name="Thumbnail"
    args={{
      src: example.squareLarge
    }}
    argTypes={{argTypes}}
    parameters={{}}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Using an horizontal image

<Preview>
  <div style={{float: 'left'}}>
    <span style={{fontWeight: 'bold'}}>Thumbnail adjusted to 450x200 px</span>
    <Thumbnail src={example.horizontalLarge} width="450px" height="200px"/>
  </div>
  <div style={{float: 'left'}}>
    <span style={{fontWeight: 'bold'}}>Thumbnail adjusted to 280x200 px</span>
    <Thumbnail src={example.horizontalLarge} width="280px" height="200px"/>
  </div>
  <div style={{width: '750px'}}>
    <span style={{fontWeight: 'bold'}}>Thumbnail adjusted to 100% (container has 750px) x 200px</span>
    <Thumbnail src={example.horizontalLarge} width="100%" height="200px"/>
  </div>
  <div>
    <span style={{fontWeight: 'bold'}}>Image component without thumbnail</span>
    <Image src={example.horizontalLarge} width="80%"/>
  </div>
</Preview>

## Using a vertical image

<Preview>
  <div style={{float: 'left'}}>
    <span style={{fontWeight: 'bold'}}>Thumbnail adjusted to 450x200 px</span>
    <Thumbnail src={example.verticalLarge} width="450px" height="200px"/>
  </div>
  <div style={{float: 'left'}}>
    <span style={{fontWeight: 'bold'}}>Thumbnail adjusted to 280x200 px</span>
    <Thumbnail src={example.verticalLarge} width="280px" height="200px"/>
  </div>
  <div style={{width: '750px'}}>
    <span style={{fontWeight: 'bold'}}>Thumbnail adjusted to 100% (container has 750px) x 200px</span>
    <Thumbnail src={example.verticalLarge} width="100%" height="200px"/>
  </div>
  <div>
    <span style={{fontWeight: 'bold'}}>Image component without thumbnail</span>
    <Image src={example.verticalLarge} width="80%"/>
  </div>
</Preview>

## Properties

<ArgsTable of={Thumbnail} />