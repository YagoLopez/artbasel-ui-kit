import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import example from './ProductTile.example.json';
import ProductTile from './ProductTile';

<Meta
  title="Components/Structure/Product Tile"
  component={ProductTile}
  decorators={[withDesign]}
/>

export const Template = (args) => <ProductTile {...args} />;

export const argTypes = {
  size: {
    table: {
      category: 'ProductTile',
    },
  },
  image: {
    table: {
      category: 'ProductTile',
    },
  },
  year: {
    table: {
      category: 'ProductTile',
    },
  },
  gallery: {
    table: {
      category: 'ProductTile',
    },
  },
  productName: {
    table: {
      category: 'ProductTile',
    },
  },
  title: {
    table: {
      category: 'ProductTile',
    },
  },
  price: {
    table: {
      category: 'ProductTile',
    },
  },
  offer: {
    table: {
      category: 'ProductTile',
    },
  },
  className: {
    table: {
      category: 'Styles',
    },
  },
  cssStyles: {
    table: {
      category: 'Styles',
    },
  },
  cssInternalPrefix: {
    table: {
      category: 'Styles',
    },
  },
};

# Product Tile

The tile is applied to represent a product in a Product List Page.

<Canvas style={{ width: '415px' }}>
  <Story
    name="Product\ Tile"
    args={{
      size: 'm',
      image: example.image,
      title: example.title,
      productName: example.productName,
      year: example.year,
      image: example.image,
      gallery: example.gallery,
    }}
    argTypes={argTypes}
    parameters={{
      design: {
        productName: 'Figma Design',
        type: 'figma',
        url:
          'https://www.figma.com/file/TENnOP4K38G6r3vDL3emCP/AB---Style-Guide ',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Preview style={{ width: '1200px' }}>
  <div>
    <div style={{ width: '375px', marginRight: '16px', float: 'left' }}>
      <ProductTile
        title={example.title}
        productName={example.productName}
        year={example.year}
        image={example.image}
        gallery={example.gallery}
      />
    </div>
    <div style={{ width: '375px', marginRight: '16px', float: 'left' }}>
      <ProductTile
        title={example.title}
        productName={example.longName}
        year={example.year}
        image={example.horizontalImage}
        gallery={example.gallery}
        price={example.price}
      />
    </div>
    <div style={{ width: '375px', float: 'left' }}>
      <ProductTile
        title={example.title}
        productName={example.productName}
        year={example.year}
        image={example.image}
        gallery={example.gallery}
        price={example.price}
        offer
      />
    </div>
  </div>
</Preview>

## Properties

<ArgsTable of={ProductTile} />
